# gaokao-analyzer 现状盘点报告（v6.1）

> 生成日期：2026-07-10
> 提取来源：git 提交历史、源码（`engines/`、`services/`、`routes/`）、`docs/`（v6.0-PRD / v6.0-architecture / system_design）、`ROADMAP.md`、`CHANGELOG.md`、数据库 `data/gaokao.db` 实际状态
> 状态：已确认（基于代码核实，非仅看 ROADMAP 勾选）

---

## 1. 项目概览

**gaokao-analyzer** 是一个面向高考的「模拟卷智能分析系统」，覆盖从试卷采集、题目结构化、质量诊断到智能组卷、多维检索的完整链路。当前版本 **v6.0**，已具备生产部署能力（Docker / PostgreSQL / Helm / CI）。

| 维度 | 现状 |
|------|------|
| 当前版本 | v6.0（git HEAD：`74b24a2 docs(v6.0)`） |
| 测试规模 | 258 个测试函数，核心模块覆盖率 ≥ 50% |
| 文档 | Docusaurus 文档站（`docs/`），含 v6.0-PRD、架构、系统设计 |
| 代码质量 | ruff + black + mypy(strict) 零错误；CI 全量阻塞 |
| 部署 | Docker / docker-compose.prod / Helm / GitHub Actions |

---

## 2. 技术栈速览

- **后端**：Python 3.13 · FastAPI（`app.py`）· SQLAlchemy · Pydantic · Celery + Redis（异步任务 / 双级缓存）
- **数据库**：PostgreSQL（生产）/ SQLite（开发），三层架构 `repositories/ → services/ → routes/`
- **分析引擎**：Numba JIT 加速的模拟核心；IRT + CTT 混合质量诊断（`engines/irt_models.py`、`hybrid_quality.py`）
- **组卷引擎**：OR-Tools CP-SAT 约束求解 + 贪心退路（`engines/composition_engine.py`）
- **前端**：`frontend/`（npm 构建 + i18n 国际化 + 液态玻璃 UI）
- **可观测 / 安全**：Prometheus 指标、JWT + RBAC、CORS 白名单、安全响应头、速率限制
- **采集**：`auto_scraper.py` + `scrape_service` + `edu_source_adapters`（多源适配器）

---

## 3. 已完成工作

### 3.1 工程基线（v5.2）
三层架构重构、操作审计日志、测试覆盖率 50%+、mypy strict、API 版本化 + JWT+RBAC、Redis 双级缓存、Celery 异步、分析器插件化、i18n、Numba JIT、Docker 化、pre-commit、CI、安全头、速率限制。

### 3.2 功能模块（v6.0）
- **T01 数据层重构**：Repository 模式 + 新表 schema + 迁移脚本
- **T02 题型分类引擎**：题型库 CRUD
- **T03 IRT+CTT 混合质量诊断体系**
- **T04 智能组卷引擎**：约束求解（OR-Tools CP-SAT）
- **T05 错题库 + 三端工作台**
- 部署增强：PostgreSQL 正式切、docker-compose.prod + healthcheck
- 采集系统增强：去重 / 自动分类 / 仪表盘
- 依赖增强：ortools / prometheus-client
- P0/P1/P2 缺口清零（安全、CI、mypy、Helm、JWT-refresh、ROADMAP、License 等）
- 文档站：Docusaurus（tagline / 首页 / 架构 / 部署）

---

## 4. v6.1 现状盘点（核心）

> 说明：ROADMAP.md 中 v6.1 五项仍为未勾选状态，但**代码核实后 4 项实际已完成**，仅「试卷数据采集」为空缺。下表为真实状态。

| # | v6.1 项 | 真实状态 | 证据 |
|---|---------|----------|------|
| 1 | CI 强化（mypy 全量阻塞 + pip-audit + 前端 build） | ✅ 已完成 | `.github/workflows/ci.yml`：`mypy .` 全量阻塞；`npm run build`；`pip-audit`（非阻塞，作警告） |
| 2 | OR-Tools 组卷引擎（激活 CP-SAT） | ✅ 已完成 | `engines/composition_engine.py` 实现 `CpModel` 约束求解 + 贪心退路；`services/composition_service.py:49` 真实调用 `.solve()`；`requirements.txt` 已声明 `ortools>=9.8.0` |
| 3 | PostgreSQL 正式切（SQLite→PG 生产迁移） | ✅ 已完成 | git `524b17c`；`docker-compose.prod.yml` + `.env.example` + `DATABASE_URL` + `scripts/migrate_to_pg.py` |
| 4 | 试卷数据采集（1000 模拟卷 + 5 年真题入库） | ❌ 未完成 | `data/gaokao.db` 仅有 schema，**0 卷入库**；`subjects/sources/papers/questions` 表为空 |
| 5 | 一键部署体验优化（Docker Compose 一键启动生产环境） | ✅ 已完成 | `docker-compose.prod.yml` + `Docker一键启动.bat`（已 `chcp 65001` 解决 Windows UTF-8 编码） |

---

## 5. 缺口分析

**唯一真实缺口 = 试卷数据采集。**

- **现象**：`gaokao.db` 表结构完整（`papers`/`questions`/`subjects`/`knowledge_points`/`question_types` 等 30+ 表就绪），但无任何数据。
- **根因**：目标数据源锁定**学科网 / 组卷网**，两者均需**付费账号登录**方可下载，当前环境无凭据，无法直接爬取真实版权卷子。
- **影响**：智能组卷、质量诊断、多维检索等功能虽代码完备，但缺少数据无法端到端演示与压测。

---

## 6. v6.1 执行计划（本次「接着完成」）

目标：把数据采集跑通，使系统具备可演示、可压测的数据底座；真实版权卷子保留导入通道待凭据后补齐。

### 6.1 合成种子数据（本次落地）
- 新增 `scripts/seed_synthetic_data.py`：按**真实 schema** 向 `gaokao.db` 灌入
  - 默认 **1000 套合成模拟卷** + **近 5 年（2021–2025）真题风格卷**（9 学科 × 多套）
  - 每卷配套题目（含 `score` / `knowledge_points` / `difficulty_tag` / `irt_a,b,c` / `question_type_id` / `content_hash`）
  - 确保 `subjects` / `knowledge_points` / `question_types` / `sources`(synthetic) 基础数据存在
- **幂等**：重跑先清除 `collector='seed-script'` 的合成行，避免重复
- **去重兼容**：生成 `content_hash`，与既有 `dedup_records` 流程一致

### 6.2 真实卷子导入通道（保留 + 说明）
- 既有 `auto_scraper.py` / `scrape_service` / `edu_source_adapters` 为多源采集框架，已具备接入点
- 用户后续提供以下任一项即可导入真实卷：
  1. 学科网 / 组卷网账号凭据（走 `edu_source_adapters` 适配）
  2. 本地导出文件（txt / 图片 / Excel）→ 新增导入脚本解析入库
- 报告与 README 中补充「真实卷导入」操作说明

### 6.3 验收标准
- [ ] `gaokao.db` 中 `papers` 数量 ≥ 目标批次（合成 1000 + 真题风格 ≥ 45）
- [ ] `composition` 组卷可对合成卷正常运行（CP-SAT 求解成功）
- [ ] 现有测试套件冒烟通过（无回归）
- [ ] `ROADMAP.md` v6.1 已完成项勾选，数据采集项标注「合成种子完成 / 真实待凭据」

---

## 7. 后续路线图（参考）

- **v6.2**：PWA 移动端适配、组卷导出（Word/LaTeX）、班级/学校级多租户、学情报告自动生成
- **v7.0**：原生移动 App、AI 辅助组卷（DeepSeek）、拍照搜题/OCR、企业版、开放平台

---

## 8. 决策记录（grill-me 对齐结果）

| 时间 | 决策 | 原因 | 影响 |
|------|------|------|------|
| 2026-07-10 | 报告 = 现状盘点报告（Markdown） | 最贴合「先提取再推进」 | 落地本项目仓库，便于 git 跟踪 |
| 2026-07-10 | 接着完成 = 清 v6.1 剩余项 | ROADMAP 既定目标 | 4 项已实质完成，聚焦数据采集 |
| 2026-07-10 | 数据采集 = 流水线 + 合成种子 | 学科网/组卷网需付费登录，无法直接爬真实卷 | 真实卷保留导入通道，待凭据补齐 |
| 2026-07-10 | 报告后本会话直接落地代码 | 用户偏好一次性全自动 | 编写 seed 脚本并灌库验证 |

---

_报告由 WorkBuddy 基于代码核实自动提取，非凭 ROADMAP 勾选臆断。_
