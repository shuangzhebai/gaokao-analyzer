apiVersion: v1
kind: Service
metadata:
  name: {{ include "gaokao-analyzer.fullname" . }}
  labels:
    {{- include "gaokao-analyzer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gaokao-analyzer.selectorLabels" . | nindent 4 }}
